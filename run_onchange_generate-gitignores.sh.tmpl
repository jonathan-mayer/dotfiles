#! /bin/bash
mkdir -p ~/.gitconfigs
rm -f ~/.gitconfigs/*
{{- range $i, $gitInstance := .git.machines}}
cat <<EOF > ~/.gitconfigs/.gitconfig-{{ $i }}
[user]
    name = {{ $gitInstance.name }}
    email = {{ $gitInstance.email }}
EOF
{{- end }}
